<form method="post" action="{{ path('app_examen_prix_delete', {'id': examen_prix.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_prix.id) }}">
    <button class="btn">Delete</button>
</form>
